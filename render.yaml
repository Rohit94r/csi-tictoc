services:
  - type: web
    name: tic-toc-server
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: CLIENT_URL
        sync: false
      - key: NODE_VERSION
        value: "20"
