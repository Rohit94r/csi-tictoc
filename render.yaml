services:
  - type: web
    name: tic-toc-server
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm ci --omit=dev
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: CLIENT_URL
        sync: false
      - key: NODE_VERSION
        value: "20"
